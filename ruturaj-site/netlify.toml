[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA fallback — every route is served by the single React entry point.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
