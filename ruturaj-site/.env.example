# Copy these into Netlify -> Site settings -> Environment variables.
# Never commit real values.

# Shared passphrase that gates the progress store and the on-demand email
# endpoint. Any reasonably long random string.
BLUEPRINT_KEY=

# Gmail SMTP credentials for the daily emails.
SENDER_EMAIL=
SENDER_PASS=
MY_EMAIL=
